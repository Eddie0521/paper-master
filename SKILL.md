---
name: paper-reader
description: 渐进式精读一篇论文——摸底、阅读计划、按层讲解、终考复述，产出费曼档案。带 URL/路径开新篇，无参数续读。
disable-model-invocation: true
---

# Paper-Reader

目的地是**精读掌握**：会话结束时，用户能用自己的话复述这篇论文的动机、方法与证据链，能回答"为什么这么设计"级别的问题。每一步都服务这个闭环——讲完不等于读完，复述通过才算。

筛读（判断值不值得读）不归本 skill，用 /read；多篇对比阅读超出范围。

**数据落位**：`~/.claude/paper-reader/`（首次使用时创建）

- `<slug>/` — 一篇论文一夹：`paper.md`、`citations/`、`plan.md`、`archive.md`
- `learning-log.md` — 全局学习履历，append-only

文件模板与 slug 规则见 [FORMATS.md](FORMATS.md)。

**讲解语言**：中文；术语、公式、变量名保留英文原文。

**提问选型**：AskUserQuestion 按选项性质选型——互斥取一（如闸门的继续/跳过/再深入）用单选；多项可同时成立（如摸底自评、批改计划时勾选多个节点）用 multiSelect 多选。

## 入口

- 带参数（arXiv/网页 URL 或本地 PDF/markdown 路径）→ 新精读，从第 1 步开始。
- 无参数 → 扫描数据目录中状态非"已终考"的论文，列出让用户挑一篇，载入其 `plan.md` 进度，从中断的节点继续（第 6 步）。

## 流程

### 1. 吸收

抓取论文全文转 markdown 存 `<slug>/paper.md`，通读。完成标准：不看原文能说出论文的主张、方法结构、主要实验及其结论。

**抓取工具**：首选本 skill 目录下的独立脚本 `bun <本skill目录>/scripts/fetch-paper.ts <URL|本地PDF路径> <输出.md>`——内置抓取级联（arXiv 自动按 官方HTML→ar5iv→PDF 择优，unpdf 解析 PDF，Readability 抽取网页，jina/defuddle 代理兜底），避开 WebFetch 读大 PDF 的 maximum call stack 报错；依赖由 bun 自动安装，首次运行需联网。脚本挂了再退 WebFetch。本地 markdown 直接拷入。第 2 步抓引文同用此工具。

### 2. 关键引文

从引用里挑 2-4 篇**关键引文**：直接前驱（本文改进/反驳的对象）、主要 baseline、所用 benchmark 的原文。抓各自摘要或相关章节存 `citations/`；抓取失败标"未验证"。其余引文靠模型知识——讲到拿不准的具体声明（数字、结论、时间线）时明示不确定。

### 3. 摸底

按固定三层骨架列候选节点：

- **前置概念** — 读懂本文必需、但论文默认读者已会的知识
- **领域格局** — 本文接的是哪段话：前序方法、综述视角、benchmark 与评价协议
- **本文** — 论文自身贡献，拆成若干讲解节点

读 `learning-log.md`，履历里学过的前置概念预标为"会"。对剩余前置概念用 AskUserQuestion（multiSelect）问："这些概念哪些你能给别人讲清楚？"

摸底的职责是**防冗余**——别把用户会的讲一遍。防漏不归它管（下潜兜底），所以问勾选、不出考题。

### 4. 阅读计划

写 `<slug>/plan.md`：三层各节点，每节点标 详/略/跳过 加一句理由（"自评会→跳过"、"履历学过→略"、"本文核心→详"）。空层省略。

把计划摆给用户批改（"跳过X""重点讲Y"），确认后开讲。此后计划就是导航图：每讲完一个节点更新进度标记——这也是断点续读的锚。精读一篇论文跨多次会话是常态，随时可停。

### 5. 速览

开讲第一件事，永不跳过：几分钟讲清这篇论文**主张什么、为什么重要、接的是哪段话**。后面每个节点的动机都挂在这里。

### 6. 按层推进

沿计划从外层往里讲。每个节点：

- **三配料**：具体例子走一遍（抽象概念/方法配数值或玩具例子）；图示（结构性内容用图——可视化工具可用时用富图，否则 mermaid/ASCII）；连接旧知识（"这是你履历里 X 的变体，差别在…"）。
- 讲完过**闸门**：AskUserQuestion 问 继续/跳过/再深入。闸门只控节奏，不考试。
- 讲解中发现用户缺一个摸底没测到的前置概念 → **下潜**：暂停主线当场补讲，补完即回，该概念记入本次学到清单。
- 更新 `plan.md` 进度标记。

每层结束过**检验**：1-2 道开放综合题，必须用户自己组织答案（选择题会退化成认读）。答错的点换角度重讲、再验，通过才进下一层。检验结果记入 `plan.md`。

### 7. 终考

请用户用自己的话完整复述：**动机 → 方法 → 证据链**。对照 `paper.md` 指出复述中的漏洞与含糊处；大漏洞回讲补上。完成标准：复述覆盖三段、无硬伤。

### 8. 归档

- 写 `<slug>/archive.md`（**费曼档案**）：用户复述原文（少编辑）+ 指出的漏洞 + 会话关键问答 + 最终计划快照。
- `learning-log.md` 追加本次学到的概念（含下潜补讲的），一行一条。
- `plan.md` 状态改"已终考"。
- 问一句：要不要用 /wiki-write 把这篇存进 wiki（可选）。
